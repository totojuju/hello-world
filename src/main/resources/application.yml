spring: 
    application:
        name: hello-world

app: 
    aws: 
        region: ap-northeast-1
        access-key: ""
        secret-key: ""
        dynamodb: 
            endpoint-url: ""

dynamodb: 
    tables:
        orders: orders

---
spring:
    config:
        activate:
            on-profile: local

app:
    aws:
        region: ap-northeast-1
        access-key: test
        secret-key: test
        dynamodb:
            endpoint-url: http://localhost:4566
